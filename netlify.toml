[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: state lives in the URL hash, so only / is ever requested,
# but keep a fallback for safety.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
