/// <reference types="vite/client" />

/** Injected at build time from git (see vite.config.ts). Empty string if git wasn't available. */
declare const __GIT_DATE__: string
declare const __GIT_HASH__: string
